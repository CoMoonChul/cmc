components:
  schemas:
    CreateCommentReqDTO:
      type: object
      required:
        - content
        - user_num
        - target_id
        - comment_target
      properties:
        content:
          type: string
        user_num:
          type: integer
          format: int64
        target_id:
          type: integer
          format: int64
        comment_target:
          type: integer
          format: int32

    CreateCommentResDTO:
      type: object
      properties:
        comment_id:
          type: integer
          format: int64
        content:
          type: string
        user_num:
          type: integer
          format: int64
        target_id:
          type: integer
          format: int64
        comment_target:
          type: integer
          format: int32
        created_at:
          type: string
        updated_at:
          type: string

    DeleteCommentReqDTO:
      type: object
      properties:
        comment_id:
          type: integer
          format: int64

    DeleteCommentResDTO:
      type: object
      properties:
        comment_id:
          type: integer
          format: int64

    UpdateCommentReqDTO:
      type: object
      properties:
        comment_id:
          type: integer
          format: int64
        content:
          type: string
        user_num:
          type: integer
          format: int64
        target_id:
          type: integer
          format: int64
        comment_target:
          type: integer
          format: int32

    UpdateCommentResDTO:
      type: object
      properties:
        comment_id:
          type: integer
          format: int64
        content:
          type: string
        user_num:
          type: integer
          format: int64
        target_id:
          type: integer
          format: int64
        comment_target:
          type: integer
          format: int32