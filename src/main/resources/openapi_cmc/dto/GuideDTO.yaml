components:
  schemas:
    GetGuideQueryResponse:
      type: object
      required:
        - id
      properties:
        id:
          type: integer
          format: int64
        description:
          type: string

    GuideDTORequest:
      type: object
      required:
        - id
      properties:
        id:
          type: integer
          format: int64

    GetGuideDTOResponse:
      type: object
      required:
        - id
      properties:
        id:
          type: integer
          format: int64
        description:
          type: string

    PostGuideCreateRequest:
      type: object
      required:
        - description
      properties:
        description:
          type: string

    PostGuideCreateResponse:
      type: object
      required:
        - description
      properties:
        id:
          type: integer
          format: int64
        description:
          type: string

    PostGuideUpdateRequest:
      type: object
      required:
        - id
        - description
      properties:
        id:
          type: integer
          format: int64
        description:
          type: string

    PostGuideUpdateResponse:
      type: object
      required:
        - id
        - description
      properties:
        id:
          type: integer
          format: int64
        description:
          type: string

    PostGuideDeleteRequest:
      type: object
      required:
        - id
      properties:
        id:
          type: integer
          format: int64

    PostGuideDeleteResponse:
      type: object
      required:
        - id
      properties:
        id:
          type: integer
          format: int64