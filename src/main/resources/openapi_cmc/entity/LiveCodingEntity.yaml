components:
  schemas:
    LiveCoding:
      type: object
      x-class-extra-annotation: "@jakarta.persistence.Entity"
      properties:
        LiveCodingId:
          type: long
          x-field-extra-annotation: |
            @jakarta.persistence.Id
            @jakarta.persistence.GeneratedValue(strategy = jakarta.persistence.GenerationType.IDENTITY)
            @jakarta.persistence.Column(columnDefinition = "BIGINT COMMENT '라이브 코딩 방 ID'")
        hostId:
          type: long
          x-field-extra-annotation: |
            @jakarta.persistence.Column(columnDefinition = "BIGINT COMMENT '방장 회원 ID'")
        createdAt:
          type: string
          x-field-extra-annotation: |
            @jakarta.persistence.Column(insertable = false, updatable = false, columnDefinition = "TIMESTAMP COMMENT '라이브 코딩 생성 일시'")
        updatedAt:
          type: string
          x-field-extra-annotation: |
            @jakarta.persistence.Column(insertable = false, updatable = false, columnDefinition = "TIMESTAMP COMMENT '라이브 코딩 방 업데이트 일시'")
